# TEST-DRIVEN DEVELOPMENT SPECIFICATION
# TriageFreshDbBootstrapFix — TDD Spec

**Document ID:** `CB-TRIAGE-FDBB-IMP-001-TEST`
**Version:** `1.0`
**Date:** `2026-07-27`
**Status:** `Implemented` — *4/4 TCs verified by actual runs 2026-07-27 (TC-INT-01/02 green, TC-INT-03 6-suite BUILD SUCCESS 15/15, TC-INT-04 full regression categorized — zero failures attributable to this change). Spec approval: project owner directive 2026-07-27 (verbal: "làm theo cách bạn cảm thấy phù hợp nhất").*
**Standard:** ISO/IEC/IEEE 29119-3:2021
**Author:** `AI Agent`
**Reviewed by:** `[x] Project owner (verbal directive 2026-07-27)`
**DPO Sign-off:** `N/A — no new PII processing`
**Approved by:** `[x] Project owner (verbal, 2026-07-27)`
**Classification:** `Internal — Confidential`

**References:**
- `04_Implement/TriageFreshDbBootstrapFix/TriageFreshDbBootstrapFix_TDS.md` (CB-TRIAGE-FDBB-IMP-001)
- `B20260724111500__canonical_70_table_baseline.sql` + post-baseline chain — persistence oracle
- `TriageService.java` / `IntakeSessionWriter.java` / `IntakeSession.java` — D3 oracle
- `TriageConsentIntegrationTest.java` tc18 comment — first written record of D3

> **Quy ước TDD:** tests written first where feasible; D3 has a live red gate. D1/D2 red evidence is HISTORICAL (the defects fail during Spring context load, before any test method — see §5.1 note) and is cited, not re-broken.

---

## CHANGELOG

| Ngày | Người thực hiện | Nội dung thay đổi |
|------|-----------------|-------------------|
| 2026-07-27 | AI Agent | Initial creation — TDD spec for TriageFreshDbBootstrapFix (D1/D2/D3); Status Approved per owner directive 2026-07-27. |
| 2026-07-27 | AI Agent — Amelia (Dev Agent) | Truthful sync to Implemented: TC-INT-01 red (500/StaleObjectState) captured in red-gate-evidence.log, green after fix; TC-INT-02 pin green pre- and post-fix; TC-INT-03 6-suite fresh-chain run BUILD SUCCESS 15/15 with all harness workarounds deleted; TC-INT-04 full regression 3166/24F/41E fully categorized in §4 (zero category-(b) failures; A/B holdout proof recorded in TDS changelog). §5 tracker, §5.1 red-gate table, §6 DoD and §8 anti-pattern review updated from ACTUAL results only. |

---

## 1. Thông tin Module

| Field | Value |
|-------|-------|
| **Feature / Gap ID** | `TriageFreshDbBootstrapFix (D1/D2/D3)` |
| **Module** | `db/migration (Flyway chain)` + `triage` |
| **Spec gốc** | `CB-TRIAGE-FDBB-IMP-001` |
| **Priority** | 🔴 P0 — fresh deploys are impossible; live 500 on a production endpoint |
| **Sprint** | `2026-07-27` |
| **Data Classification** | `Internal` |
| **Compliance Scope** | `N/A` (BR-SAFETY preservation constraint C4 applies) |
| **Upstream Dependencies** | Docker/Testcontainers (available on this host, verified 2026-07-27) |
| **Downstream Consumers** | every `AbstractPostgresIntegrationTest` subclass; fresh deployments |

### 1.1 AI Generation Context (CASE 2.0)

| Field | Value |
|-------|-------|
| **AI Assisted?** | `Yes` |
| **Constraint Source** | `CB-TRIAGE-FDBB-IMP-001 §17 (C1–C6)` |
| **Constraints Injected** | immutability of applied migrations; guarded no-ops; verbatim DDL; no safety-assertion weakening; writer contract preserved; harness workarounds removed |
| **Model** | `Claude (Fable 5) — Claude Code` |
| **Trust Level** | `T2 → T3 (Red Gate for TC-INT-01) → T4 after actual green runs` |

---

## 2. Logic Issues Resolved

| # | Spec gốc (sai / thiếu) | Thực tế (schema / policy) | Fix áp dụng trong test |
|---|------------------------|--------------------------|------------------------|
| L1 | Post-baseline migrations assume pre-baseline bridge tables always exist | Baseline path skips their creators → context-load failure on EMPTY DBs | TC-INT-03 proves the ONLY Flyway location is `classpath:db/migration` (no init script, no testfix) and contexts still boot |
| L2 | `V20260724212450` demands exactly 1 story65 state row | 0 rows on baseline path → `RAISE EXCEPTION` | synthetic row `('V20260722019950', false, false)` → its checks truthfully assert no legacy shadow tables (true on baseline); NOT bypassed |
| L3 | `V20260724214200` demands legacy + canonical expert columns | baseline is end-state (canonical only) | `V20260724214150` re-adds nullable legacy columns; `214200` drops them → net-zero |
| L4 | `startConversation` builder pre-assigns id on `@GeneratedValue` entity | non-null id → Spring Data `merge()` → `StaleObjectStateException` on real PG when `clientRequestId` absent | TC-INT-01 pins the 2xx contract; id now generated by JPA on the save() branch; TC-INT-02 pins the untouched native-insert branch |

---

## 3. Test Design Specification (TDS)

### TDS-01 — Scope

```
Integration only (Testcontainers PostgreSQL, real Flyway chain, MockMvc + real services;
ChildTriageAiClient/GeminiTriageClient/Email/Sms/Emergency/ConsentService mocked — no external calls).
Unit layer: N/A — D1/D2 are pure SQL-chain defects; D3's service logic is already unit-covered
by TriageServiceTest et al., whose JPA-like save() stubs remain valid (TDS §1 source baseline).
```

### TDS-02 — Test Basis

| Source | Items Derived |
|--------|--------------|
| `CB-TRIAGE-FDBB-IMP-001 §3` | ADR-TFBF-001..003 behaviors |
| `BR-FDBB-001..007` | fresh bootstrap, guard no-ops, contract preservation |
| `IntakeController.java :61-68` | endpoint + 200 envelope oracle |
| `TriageService.java :304-361` | session-creation branches |
| chain files `210000/211500/212450/214200` | consumption/drop assertions |

### TDS-03 — Test Conditions and Coverage Items

| Condition ID | Test Condition | Coverage Item | Test Cases |
|-------------|---------------|---------------|-----------|
| TC-COND-001 | start WITHOUT clientRequestId persists via JPA-generated id → 2xx | `TriageService.startConversation` save() branch | TFBF-TC-INT-01 |
| TC-COND-002 | start WITH clientRequestId: DB-arbitrated insert + replay idempotency unchanged | writer branch | TFBF-TC-INT-02 |
| TC-COND-003 | empty DB boots the REAL chain (no init script / testfix) | both new migrations + reverted base class | TFBF-TC-INT-03 |
| TC-COND-004 | legacy chain path & replay tests keep byte-identical behavior (guarded no-ops) | migration-replay suites in full regression | TFBF-TC-INT-04 |

### TDS-04 — Test Techniques

| Technique (ISO 29119-4) | Applied To | Rationale |
|------------------------|------------|-----------|
| Equivalence Partitioning | `clientRequestId` present/absent | the two persistence branches |
| State Transition Testing | Flyway history states (fresh / mid-chain / fully-migrated) | guard correctness |
| Error Guessing | replay with same clientRequestId; guard on failed-history rows | idempotency + repair path |

### TDS-05 — Test Data Requirements

| Fixture ID | Type | Value / Logic | Mục đích |
|-----------|------|---------------|---------|
| `FX-TFBF-001` | DB seed | fresh MOTHER user per test (unique email, `userRepository.save`) — same pattern as `TriageConsentIntegrationTest.saveUser` | JWT principal |
| `FX-TFBF-002` | Mock | `ITriageConsentService` no-op mock (consent gate out of scope — §6-sanctioned sibling fixture) | reach the persistence code |
| `FX-TFBF-003` | Mock | `ChildTriageAiClient.startIntake(any())` → SYNTHETIC `ASK_MORE` JSON (sibling `ASK_MORE_START` shape) | no Python/Gemini call |
| `FX-TFBF-004` | value | `clientRequestId = UUID.randomUUID().toString()` (36 chars, matches `^[A-Za-z0-9_-]{16,64}$`) | TC-INT-02 |

All test data SYNTHETIC.

---

## 4. Test Case Specification

### TFBF-TC-INT-01 — startConversation WITHOUT clientRequestId → 200, session persisted

**Severity:** `CRITICAL`
**Feature Under Test:** `POST /api/v1/triage/intake/conversation/start` (no `clientRequestId`)
**Test File:** `src/test/java/com/carebridge/backend/triage/TriageConversationStartIntegrationTest.java`
**TDD Phase:** 🟢 GREEN *(red 2026-07-27: `Status expected:<200> but was:<500>` — ObjectOptimisticLockingFailureException wrapping StaleObjectStateException, see red-gate-evidence.log; green 2026-07-27 after ADR-TFBF-003 fix)*
**Condition Ref:** `TC-COND-001`
**Oracle Source:** `IntakeController :61-68` (200 + `ApiResponse.success`) / BR-FDBB-006 — the endpoint contract, not the buggy behavior.

**Preconditions:** FX-TFBF-001/002/003; Testcontainers PostgreSQL up; real Flyway chain applied.

**Test Steps:**
1. Arrange: seed MOTHER, mint JWT, stub `startIntake` → ASK_MORE.
2. Act: `POST .../conversation/start` with body `{"initialText":"Bé sốt nhẹ — SYNTHETIC"}` (NO clientRequestId).
3. Assert: HTTP 200; `$.success=true`; `$.data.status=ASK_MORE`; `$.data.intakeSessionId` is a UUID; DB has exactly 1 `triage_sessions` row for the user with `client_request_id IS NULL` and `triage_session_id = $.data.intakeSessionId`.

**Expected Result (PASS):** as asserted above.
**Expected Result (FAIL — dấu hiệu lỗi):** HTTP 500 (`ObjectOptimisticLockingFailureException`/`StaleObjectStateException` from the merge path), no row visible via the response.

**Current Status:** 🟢 Passing *(actual run 2026-07-27, Testcontainers postgres:16-alpine, real chain)*
**Implementation Note:** fix per ADR-TFBF-003 (id moves out of the builder into the databaseArbitrated branch).

---

### TFBF-TC-INT-02 — startConversation WITH clientRequestId stays idempotent (regression pin)

**Severity:** `HIGH`
**Feature Under Test:** DB-arbitrated branch (`IntakeSessionWriter.insertConversationIfAbsent`) + replay
**Test File:** `TriageConversationStartIntegrationTest.java`
**TDD Phase:** 🟢 GREEN *(pre-fix pin PASSED on 2026-07-27 red run as documented in §5.1; still green after the fix — writer path byte-identical)*
**Condition Ref:** `TC-COND-002`
**Oracle Source:** `IntakeSessionWriter :20-54` + `TriageService :339-354` replay contract (BR-FDBB-006).

**Test Steps:**
1. Same fixtures; `clientRequestId` = FX-TFBF-004.
2. `POST .../conversation/start` twice with the SAME body (same clientRequestId).
3. Assert: both 200; both return the SAME `intakeSessionId`; exactly 1 `triage_sessions` row for the user with that `client_request_id`.

**Expected Result (PASS):** replay returns the stored envelope; single row.
**Expected Result (FAIL):** duplicate rows or differing session ids → writer path broken by the fix (forbidden by C5).

**Current Status:** 🟢 Passing *(actual runs 2026-07-27: pre-fix pin run AND post-fix 6-suite run)*

---

### TFBF-TC-INT-03 — Fresh empty DB boots the REAL chain (workarounds deleted)

**Severity:** `CRITICAL`
**Feature Under Test:** `V20260724120000` + `V20260724214150` + reverted `AbstractPostgresIntegrationTest`
**Test File:** command-level oracle (context bootstrap of 6 suites):
`./mvnw test -Dtest='TriageConsentIntegrationTest,TriageHealthMemoryContextIntegrationTest,TriageFollowUpIntegrationTest,TestcontainersSmokeTest,LoginIntegrationTest,TriageConversationStartIntegrationTest'`
**TDD Phase:** 🟢 GREEN *(historical red per §5.1; green 2026-07-27: BUILD SUCCESS — Tests run: 15, Failures: 0, Errors: 0 across all 6 suites; Flyway log confirms fresh path B20260724111500 → V20260724120000 → 210000…214100 → V20260724214150 → 214200 → 20260726150000 with zero bridge/CANONICAL_EXPERT_REFERENCE errors)*
**Condition Ref:** `TC-COND-003`
**Oracle Source:** BR-FDBB-001/007; the base class MUST contain no init script and `spring.flyway.locations=classpath:db/migration` only.

**Expected Result (PASS):** all 6 suites green on a fresh container with ONLY the real chain.
**Expected Result (FAIL):** `FlywayMigrateException` at `V20260724210000` (`relation ... does not exist`) or `V20260724214200` (`CANONICAL_EXPERT_REFERENCE ...`) during context load — the exact historical failures.

**Current Status:** 🟢 Passing *(actual run 2026-07-27; workarounds fully deleted before the run — verified no references remain in src/test)*

---

### TFBF-TC-INT-04 — Full regression: legacy chain path untouched

**Severity:** `HIGH`
**Feature Under Test:** guard no-ops on the legacy/replay path
**Test File:** `./mvnw clean test` (notably `CanonicalSafetyActionInvariantMigrationIntegrationTest`, `CanonicalExpertProfileReferenceMigrationIntegrationTest`, which replay the chain with raw Flyway targets)
**TDD Phase:** 🟢 GREEN *(executed 2026-07-27 — no failure attributable to this change; see analysis below)*
**Condition Ref:** `TC-COND-004`
**Oracle Source:** BR-FDBB-003; known pre-existing failures: `ChecklistTemplateMigrationTest.uc82_69_int_005` (SHA drift) + any suite failing on its OWN pre-existing defects (report, do not fix).

**Expected Result (PASS):** no NEW failure attributable to this change.
**Current Status:** 🟢 Verified — zero category-(b) failures *(actual run 2026-07-27)*

**Actual regression results (2026-07-27, `./mvnw clean test`, Docker available):**
`Tests run: 3166, Failures: 24, Errors: 41, Skipped: 22` (18:12 min). Pre-Docker baseline for comparison: 3031 tests, 1 F (known SHA drift) + 75 E (ALL Docker-unavailable context errors) + 100 skipped — i.e. ~75 Testcontainers suites executed for real for the FIRST time in this run.

* **All 6 feature-relevant suites GREEN in this run** (TriageConsent 7/7, HealthMemory 2/2, FollowUp 2/2, ConversationStart 2/2, Smoke 1/1, Login 1/1).
* **New migrations applied cleanly in every containerized context**: `20260724120000` applied 26×, `20260724214150` 9×, zero migration failures; end-state inventory assertions in `FinalPhase2SchemaIntegrationTest`/`CanonicalWardMigrationIntegrationTest` list NO `carebridge_migration_bridge` objects and NO `expert_profile_id` columns → net-zero schema confirmed on real bootstraps.
* **Category (c) — known:** `ChecklistTemplateMigrationTest.uc82_69_int_005_v1RemainsByteIdentical` (SHA drift, pre-existing).
* **Category (b) — caused by this change: NONE.** Every failing test either runs at a replay position where the new migrations are never applied, or fails on objects this change does not touch (see (a) families). A/B holdout run (both new migrations removed, `CanonicalSafetyActionInvariant` + `CanonicalExpertProfileReference` re-run) documented in the TDS changelog.
* **Category (a) — pre-existing defects in suites that never ran before Docker was enabled** (23 F + 41 E across 27 suites), four root-cause families:
  * **(a-1) Baseline-commit replay semantics** (pre-session commit `794bd72c`): on an EMPTY container, `Flyway.target(X)` with `X < 20260724111500` now applies ONLY the B-baseline (log-proven: "Successfully applied 1 migration ... v20260724111500"), so every legacy-worldview replay lands in canonical end-state → `relation <legacy> does not exist` (`intake_sessions`, `uploaded_files`, `content_reports`, `provinces`, `partner_services`, `commission_config`, `mother_baseline_contexts`, `safety_monitoring_config`, `user_profiles`, `community_questions`, `reminders`, `triage_assessments`, bridge tables at pre-baseline positions, legacy `expert_profile_id` columns below 214150) and `users.person_id` NOT NULL fixture violations. Suites: Wave1–Wave9 cleanup ×9, ConsultationPaymentCleanup, PartnerSponsoredCleanup, RemainingUnusedCleanup, CareFacilityCanonical, CanonicalTriageMigration, CanonicalTriageLifecycleIntegrity, Story68FlywayBranchHistory, ContentReportFlywayBranchHistory, TriageExpertHandoffSchemaRed, CanonicalSafetyActionInvariant (tests 1–2 only; its 4 baseline-path tests PASS thanks to this fix), CanonicalExpertProfileReference (test 1 only; test 2 PASSES thanks to this fix), CommunityTopicIntegrationTest (fixture inserts `users` without `person_id`).
  * **(a-2) AI-moderation table drift** (other session's committed `V20260726100000`): `ai_content_assessments`/`ai_content_scan_jobs`/`ai_moderation_policies` break canonical-inventory counts. Suites: FinalPhase2Schema, Postgresql18CanonicalBaseline (69→75, 70→76), Postgresql18CanonicalSchema, CanonicalWard `forwardMigration...SeventyPlusThreeTables`.
  * **(a-3) Baseline filename policy**: `B20260724111500__canonical_70_table_baseline.sql` fails `CanonicalWardMigrationIntegrationTest.repositoryHasUniqueVersions...`'s `V`-prefix regex (the two NEW files match the pattern and are version-unique — verified).
  * **(a-4) Known data/seed defects**: `DevDataSeederPostgresIntegrationTest` — `mother_journeys.care_subject_id` NOT NULL seeder crash (documented pre-existing startup blocker); `LifecycleContentPostgresIntegrationTest.uc82_69_int_005` — checklist content-cardinality drift (sibling of the (c) SHA drift).

---

## 5. Red-Green-Refactor Tracker

| TC ID | Test File | 🔴 RED confirmed | 🟢 GREEN (commit) | 🔵 REFACTOR note |
|-------|-----------|-----------------|-------------------|------------------|
| `TFBF-TC-INT-01` | `TriageConversationStartIntegrationTest.java` | `[x]` *(2026-07-27: 500 / StaleObjectStateException — red-gate-evidence.log)* | `Passed (working tree — NO-COMMIT directive; 6-suite run 2026-07-27)` | none |
| `TFBF-TC-INT-02` | `TriageConversationStartIntegrationTest.java` | `[x]` *(pre-fix pin PASSED as expected — documented deviation, not a red)* | `Passed (same runs)` | none |
| `TFBF-TC-INT-03` | 6-suite command | `[x]` *(historical red — see §5.1 row)* | `Passed (BUILD SUCCESS, 15/15, 2026-07-27)` | none |
| `TFBF-TC-INT-04` | `./mvnw clean test` | `[x]` *(observational)* | `Passed — zero category-(b) failures (3166 run, 2026-07-27; see §4 TC-INT-04 analysis)` | none |

### 5.1 Red Gate Protocol (CASE 2.0 — GATE-2)

**Stub for Red Phase:** `N/A deviation (documented):` the "stub" is the CURRENT DEFECTIVE PRODUCTION CODE itself — TC-INT-01 runs against unmodified `TriageService` (pre-assigned id) and must fail with 500. Writing a throw-stub would require modifying the very code under repair before the red run.

**Red Gate Verification:**

| TC ID | Stub Result | Expected | Actual | Root Cause (nếu PASS bất thường) |
|-------|-------------|----------|--------|----------------------------------|
| `TFBF-TC-INT-01` | current defective code (merge path) | 🔴 FAIL | ☑ FAIL ☐ PASS *(2026-07-27: `Status expected:<200> but was:<500>`)* | — |
| `TFBF-TC-INT-02` | current code (writer path works) | 🟢 PASS *(intentional pre-fix pin — NOT AP-AI-002: it pins the branch that must NOT change; documented deviation)* | ☐ FAIL ☑ PASS *(as intended)* | — |
| `TFBF-TC-INT-03` | — | 🔴 HISTORICAL | ☑ FAIL (historical) | D1/D2 fail during Spring context load — no test method can execute. Historical red evidence: (1) exact Flyway errors quoted verbatim in the retired workaround headers `src/test/resources/testsupport/bridge-bootstrap.sql` :12-15 and `src/test/resources/db/testfix/V20260724214150__testfix_restore_legacy_expert_columns.sql` :13-16; (2) `04_Implement/TriageDisclaimerConsent/red-gate-evidence.log` documents the era when every `AbstractPostgresIntegrationTest` context died class-level; (3) the very existence of the committed harness workarounds (commit `1a95d9fb`) is the fix-forward record. Re-breaking the harness to reproduce was rejected as pure ceremony. |
| `TFBF-TC-INT-04` | — | observational | ☐ | |

**Red Gate Evidence:**

- Stub commit hash: `working tree (no commit — NO-COMMIT directive)`
- Tất cả FAIL? [x] Yes *(every TC expected to fail DID fail: TC-INT-01 red 500 confirmed; TC-INT-02's pass is the documented pre-fix pin deviation, TC-INT-03's red is historical — both per this table)* → **GATE-2 PASS** (T2→T3)
- Log file: `04_Implement/TriageFreshDbBootstrapFix/red-gate-evidence.log`

---

## 6. Entry / Exit Criteria

### Entry Criteria

- [x] TDS `CB-TRIAGE-FDBB-IMP-001` Approved (owner directive 2026-07-27)
- [x] Logic Issues (§2) confirmed against live chain + code (TDS §1 source baseline)
- [x] Docker available for Testcontainers (verified 2026-07-27)
- [x] Version numbers `20260724120000` / `20260724214150` collision-free across the whole tree

### Exit Criteria (DoD)

- [x] TFBF-TC-INT-01 red run captured (HTTP 500 / StaleObjectState) in `red-gate-evidence.log` *(2026-07-27)*
- [x] TFBF-TC-INT-01 + 02 green after fix (actual run 2026-07-27: 2/2 in the 6-suite run)
- [x] TFBF-TC-INT-03: 6-suite command green with workarounds fully removed (actual run 2026-07-27: BUILD SUCCESS, 15/15)
- [x] TFBF-TC-INT-04: `./mvnw clean test` — no NEW failures vs known pre-existing set (actual run 2026-07-27: 3166/24F/41E — every F/E categorized in §4 TC-INT-04; zero attributable to this change)
- [x] Harness workaround files deleted; `AbstractPostgresIntegrationTest` reverted *(no references remain in src/test — grep-verified)*
- [x] No business logic added to controllers; no interface changes (TDS §8/§9 N/A holds — only `TriageService.startConversation` internals changed)

### Suspension Criteria

- Docker daemon unavailable mid-run; PostgreSQL container port exhaustion (retry after `docker ps` cleanup).

---

## 7. Rollback Plan

See TDS §12 (same steps; test-only artifacts additionally: `git checkout -- src/test/...` for the new IT and restored workarounds).

---

## 8. CASE 2.0 Anti-Pattern Detection (AI-Assisted TCs)

| AP-ID | Anti-Pattern | Dấu hiệu trong TDD spec | Check | Gate chặn |
|-------|-------------|--------------------------|-------|-----------|
| AP-AI-001 | Unconstrained Generation | TC không reference ADR/TDS constraint nào | ☐ | G-0 |
| AP-AI-002 | Green-from-Birth | Test PASS với stub (§5.1) | ☐ *(TC-INT-02 pre-fix pin is a documented deviation, not AP-AI-002)* | G-2 ★ |
| AP-AI-003 | Implicit Decision | Test assume architecture decision không có ADR | ☐ | G-1 |
| AP-AI-004 | Layer Violation | Test verify controller có business logic | ☐ | G-4 |
| AP-AI-005 | Hallucinated Contract | Test import service/type không tồn tại | ☐ | G-3 |

**Kết quả review:**

- [x] Không phát hiện anti-pattern nào → TDD spec approved *(post-implementation verification 2026-07-27: TC-INT-01 proven non-tautological by the live red run; TC-INT-02's stub-pass is the documented pre-fix pin deviation; no hallucinated contracts — all referenced classes/migrations exist and were exercised in actual runs)*

| AP detected | TC ID | Mô tả | Fix action | Fixed? |
|------------|-------|-------|------------|--------|
| — | — | — | — | ☐ |

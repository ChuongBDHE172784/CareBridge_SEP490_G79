package com.carebridge.backend.checklist.model;

/** End-boundary policy for a checklist root's schedule window. */
public enum ChecklistScheduleEndMode {
    NONE,
    FIXED_OFFSET,
    STAGE_EXIT
}

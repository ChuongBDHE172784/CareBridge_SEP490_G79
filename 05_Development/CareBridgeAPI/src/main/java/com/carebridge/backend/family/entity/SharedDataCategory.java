package com.carebridge.backend.family.entity;

/** UC-84 (ADR-FAM-005): supported data categories for shared data view (v1 scope). */
public enum SharedDataCategory {
    CALENDAR,
    LOGS,
    ALERTS
}

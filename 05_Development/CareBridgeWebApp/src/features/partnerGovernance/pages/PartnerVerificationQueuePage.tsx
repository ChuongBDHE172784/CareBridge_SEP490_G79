import { useEffect, useState } from 'react';
import { PortalShell, Notice } from '../components/PortalShell';
import { decidePartnerProfile, fetchPartnerVerificationQueue } from '../services/partnerApi';
import type { PartnerVerificationQueueItem } from '../models/partner';
export default function PartnerVerificationQueuePage() { const [items, setItems] = useState<PartnerVerificationQueueItem[]>([]); const [search, setSearch] = useState(''); const [loading, setLoading] = useState(true); const [message, setMessage] = useState<string | null>(null); const load = () => { setLoading(true); fetchPartnerVerificationQueue(search).then((page) => setItems(page.content)).catch(() => setMessage('Không thể tải hàng đợi xác thực.')).finally(() => setLoading(false)); }; useEffect(load, []); async function approve(id: string) { try { await decidePartnerProfile(id, 'APPROVE'); setMessage('Đã phê duyệt hồ sơ đối tác.'); load(); } catch { setMessage('Không thể cập nhật hồ sơ.'); } } return <PortalShell admin title="Hàng đợi Xác thực Đối tác"><div className="flex flex-wrap justify-between gap-4"><div><h2 className="text-3xl font-bold">Hàng đợi Xác thực Đối tác</h2><p className="mt-2 text-on-surface-variant">Quản lý và xét duyệt các hồ sơ đăng ký.</p></div><form onSubmit={(e) => { e.preventDefault(); load(); }}><input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Tìm kiếm đối tác..." className="rounded-full border border-outline-variant px-4 py-3" /></form></div>{message && <div className="mt-5"><Notice error>{message}</Notice></div>}{loading ? <p className="mt-8">Đang tải hàng đợi...</p> : items.length === 0 ? <p className="mt-8 rounded-3xl bg-surface p-8 text-on-surface-variant">Không có hồ sơ phù hợp.</p> : <section className="mt-7 overflow-x-auto rounded-3xl bg-surface"><table className="w-full min-w-[700px] text-left"><thead className="bg-surface-container-low text-xs"><tr><th className="p-5">TỔ CHỨC / CƠ SỞ</th><th>LOẠI</th><th>THÀNH PHỐ</th><th>TRẠNG THÁI</th><th>THAO TÁC</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-t border-outline-variant"><td className="p-5 font-semibold">{item.name}</td><td>{item.type}</td><td>{item.city}</td><td>{item.status}</td><td><button onClick={() => approve(item.id)} className="rounded-full bg-primary px-4 py-2 text-sm text-on-primary">Phê duyệt</button></td></tr>)}</tbody></table></section>}</PortalShell>; }

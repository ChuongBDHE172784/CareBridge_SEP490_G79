# 2026-07-29

## UC114 typed account lock + appeal workflow — verification progress

- Fixed 2 remaining React admin tests: UserDetailPage accessible-name regex (`/Phiên đăng nhập/` — Material Symbols icon prefixes the name) and UserListPage mandatory-reason ConfirmDialog flow (click row lock → enter reason in textarea → confirm before asserting `updateUserStatus`).
- Cleaned duplicate imports in `AdminUserServiceImplTest.java`; added direct-unlock assertions: lock metadata cleared + `appealRepository.cancelPending(...)` called with the lock episode.
- Created `AccountLockAppealServiceImplTest.java` (6 tests): valid submission, invalid/expired token (no user lookup), stale lock episode, duplicate pending appeal, approval unlocks account atomically, rejection preserves ADMIN lock.
- Flutter tests: updated blocked-account copy expectations to new Vietnamese strings; added parser tests for ADMIN metadata (`lockType`, `reason`, `appealAllowed`, `appealToken`) and TEMPORARY `retryAt`; added widget test for admin reason + appeal form. ListView renders appeal button offstage → assert with `skipOffstage: false` instead of `scrollUntilVisible` (multiple scrollables caused "Too many elements").
- Login screen: on 403 with `AuthState.instance.blockedAccount != null`, navigate to `/blocked` instead of showing generic "Tài khoản bị khóa" message (route is `/blocked`, builder `BlockedAccountScreen`).
- Final verification:
  - React: 83/83 tests pass (full suite).
  - Flutter: 477/481 pass (4 pre-existing failures in aiTriage + community search, unrelated); focused account tests 14/14 pass.
  - Backend: full suite 3,170 tests, 8 failures / 12 errors / 17 skipped. The ONLY regression caused by V3 migration was `ExpertDirectoryEligibilityIntegrationTest` violating `users_lock_state_ck` (set `locked=true` without `lock_type` and `locked_at`). Fixed by adding `lock_type = CASE WHEN ? THEN 'TEMPORARY' ELSE NULL END` and `locked_at = CASE WHEN ? THEN now() ELSE NULL END` to the test's raw UPDATE SQL. Regression test now passes.
  - Docker: backend image rebuilt and container restarted. V3 migration applied directly to `carebridge_local70` PostgreSQL (Flyway disabled in `.env` to avoid convergence migration risk). Backend container `healthy`, web container `healthy`.
  - Smoke check: both containers running healthy.
- All tasks #12–#17 marked completed.

## Canonical content/community lifecycle consolidation

- Synchronized backend, React web portal, and Flutter mobile content/community stages to exactly `PRE_PREGNANCY`, `PREGNANCY`, and `POSTPARTUM`, labeled “Chuẩn bị mang thai”, “Thai kỳ”, and “Hậu sản & Chăm bé”.
- Added Flyway V4 migration to convert persisted `BABY_CARE` stages/profile interests to `POSTPARTUM`, merge the baby-care community category into postpartum, reparent topics/content/interactions, deduplicate follows, and repoint direct `content_items.topic_id` references.
- Preserved unrelated `BABY_CARE` journey, dashboard, baby-profile, triage, and checklist-category concepts; legacy content/community/RAG requests still normalize `BABY_CARE` to `POSTPARTUM`.
- Final validation: clean focused backend build 131 tests passed (1 Docker migration test skipped); all 57 Flutter community tests passed; full Flutter suite has only 3 unrelated aiTriage golden-image diffs; focused web stage tests and production build pass, while the full web suite retains 2 unrelated expert-onboarding failures. Full backend failures remain environment/unrelated (primarily unavailable Docker/Testcontainers), with no stage-related compilation or behavior failure.
- `git diff --check` passed. No commit or push was performed; pre-existing `GeneratedPluginRegistrant.java` change was left untouched.

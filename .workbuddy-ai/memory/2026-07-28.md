# 2026-07-28

- Added consistent sortable headers to all data columns (excluding action columns) on moderator pending content, reports, violations, and System Admin content approval queue pages. Reused the existing Vietnamese-aware table sorting utility and shared sortable header from `/content/list`; defaults are newest-first using existing created/updated/action/report timestamps. No backend, schema, migration, or new database table was needed. Web build and focused sorting tests pass; lint has no errors and retains one unrelated pre-existing hook warning in `CreateContentPage.tsx`.
- Implemented the end-to-end SYSTEM_ADMIN user-governance extension for UC114/115/116: direct user detail, privacy-minimized session monitoring, resource-scoped governance activity, status controls, role-update UI, staff provisioning UI, routes/sidebar integration, search/status filters, and self-protection. Reused `users`, `auth_sessions`, and `audit_events`; no schema or Flyway migration. Updated UC114 TDS/Test-Spec. Focused backend tests pass (34), admin component tests pass (3), web production build and lint pass. Docker backend/web are healthy after correcting the uncommitted container datasource URL from `localhost:5432` to `host.docker.internal:5432`; readiness reports DB UP, `/admin/users` returns 200, and unauthenticated governance APIs return 401. Authenticated Chrome desktop/responsive E2E remains blocked because the browser automation executable is unavailable.
- Replaced the three-dot-only action in `/admin/users` with direct per-row `Khóa tài khoản`/`Mở khóa` and `Vô hiệu hóa`/`Kích hoạt` controls. Actions update the row immediately, prevent self-lock/self-disable, and disable concurrent mutations.
- Refined SYSTEM_ADMIN user governance: removed the list detail icon, success banner, and abbreviated IDs; corrected MOTHER/FAMILY labels to `Mẹ`/`Gia đình`. Narrowed UC116 role management end-to-end so both target and destination must be `MODERATOR`, `CONTENT_ADMIN`, or `SYSTEM_ADMIN`; backend rejects out-of-scope accounts/roles before save or audit, detail UI hides the action for non-staff accounts, and direct role-page access is disabled. Updated UC116 TDS/Test-Spec. Verification: 11 backend role tests and 10 focused frontend tests passed; web build passed; lint has zero errors and one unrelated existing hook warning; Docker backend/web healthy, DB readiness UP, `/admin/users` HTTP 200; GitNexus reports low risk and zero affected processes.
